name: 'build'
description: 'build application'

inputs:
  app-name:
    description: 'my-app'
    required: true

runs:
  using: 'node12'
  steps:
    - run: echo "test build app ${{ inputs.app-name }}" 