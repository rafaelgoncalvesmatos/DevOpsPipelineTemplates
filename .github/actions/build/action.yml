name: 'build'
description: 'build application'

inputs:
  app-name:
    description: 'my-app'
    required: true

runs:
  steps:
    - name: echo test
      shell: bash
      run: echo ${{ inputs.app-name }}