name: Template Workflow

inputs:
  my_variable:
    required: true

runs:
  using: "composite"
  steps:      
    - name: echo command 
      shell: bash
      run: echo ${{ inputs.my_variable }}

    - name: Verify commit
      run: git branch --remote --contains | grep origin/main