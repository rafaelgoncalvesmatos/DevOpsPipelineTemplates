name: 'build'
description: 'build application'

inputs:
  environment:
    description: 'my-app'
    required: true

runs:
  using: 'node12'
  steps:
    - run: echo "Deploying to environment: ${{ inputs.environment }}"