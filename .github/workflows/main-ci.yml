name: Main Pipeline

on:
  push:
    branches: [main]

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      # - uses: ./.github/workflows/template.yml
      - uses: ./.github/workflows
        with:
          my_variable: "value_from_main"
         