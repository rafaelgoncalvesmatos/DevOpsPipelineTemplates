name: Test Pipeline Template

on:
  workflow_call:
    inputs:
      name:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Test
      run: |
        echo "Hello {{ inputs.name }}!"
    - name: Test 2
      run: |
        echo "Hello World!"
    - name: Test 3
      run: |
        echo "Hello World!"
    - name: Test 4
      run: |
        echo "Hello World!"